# 토큰 사용량 실측 프로토콜 — n8n Orchestration Kit 유무 비교

## 현재 상태

이 문서 작성 시점(2026-09-12)에는 **실측 결과가 없습니다.** 이유는 다음과 같습니다.

- 킷 파일(`.claude/agents`, `.claude/skills`, `.claude/commands`)이 이 Mac에 없음
- 로컬 n8n 인스턴스와 n8n-mcp 서버가 등록되어 있지 않음
- 따라서 "하네스 있음" 조건을 한 번도 실행할 수 없음

아래 프로토콜과 `measure-tokens.py`는 즉시 실행 가능한 상태로 준비했고, 스크립트는 이 프로젝트의 실제 세션 기록으로 동작을 검증했습니다. 킷과 n8n이 준비되면 그대로 돌리면 됩니다.

## 측정 1 (완료): 상시 로드되는 스킬 메타데이터 크기

2026-09-13, 발표자 로컬 설치본 기준. 하네스 없이도 잴 수 있는 유일한 항목이라 먼저 쟀다.

| 대상 | 스킬 수 | `description` 합계 | 추정 토큰 (÷4) | 본문 합계 | 본문 추정 토큰 |
|------|---------|--------------------|----------------|-----------|----------------|
| 로컬 설치된 범용 프레임워크 플러그인 | 37 | 5,876자 (평균 158자, 1,536자 상한 적용) | ≈ 1,469 | 447,011자 | ≈ 111,752 |
| 이 킷 (추정) | 10 | 명세의 설명 1개가 191자 → 10개 ≈ 1,900자 | ≈ 500~1,900 (한글 비율에 따라) | 파일 없음 | 측정 불가 |

해석:
- Claude Code 공식 문서상 스킬 본문은 호출 시에만 로드되고, 목록에는 설명만 실린다. 따라서 세션당 상시 비용은 양쪽 모두 수천 토큰 이하로 **차이가 작다.**
- "스킬이 많으면 컨텍스트가 무겁다"는 주장은 토큰 수로는 근거가 약하다. 실제 위험은 관련 없는 스킬이 잘못 트리거되는 판단 혼선 쪽이며, 이것은 토큰이 아니라 트리거 테스트(should / should-not)로 잰다.
- 토큰 절감의 주된 후보는 아래 측정 2에서 재는 "검증 통과까지의 총 토큰"과 "수정 요청 수"다.

측정 명령:

```bash
python3 - <<'EOF'
import glob,os,re
base=os.path.expanduser('~/.claude/plugins/cache/<플러그인 경로>/skills')
tot_desc=tot_body=n=0
for f in sorted(glob.glob(base+'/*/SKILL.md')):
    s=open(f,encoding='utf-8',errors='ignore').read()
    m=re.match(r'^---\n(.*?)\n---\n(.*)$',s,re.S)
    front,body=(m.group(1),m.group(2)) if m else ('',s)
    d=re.search(r'^description:\s*(.*?)(?=^\w+:|\Z)',front,re.S|re.M)
    desc=(d.group(1) if d else '').strip()[:1536]
    n+=1; tot_desc+=len(desc); tot_body+=len(body)
print(n, tot_desc, tot_desc//4, tot_body, tot_body//4)
EOF
```

## 측정 2 (예정): 검증 통과까지의 총 토큰 — 측정 원칙 (수치가 왜곡되지 않게)

1. **비교 단위는 "검증을 통과한 올바른 워크플로우에 도달할 때까지의 총 토큰"** 이다. 요청 한 번의 토큰이 아니다. 하네스는 요건 확인·리서치·검증을 추가하므로 첫 요청 하나만 비교하면 하네스가 더 많이 쓰는 것으로 나온다. 재작업·재질문·수정 요청까지 포함해야 공정하다.
2. **같은 모델, 같은 n8n 인스턴스, 같은 초기 워크플로우** 로 두 조건을 돌린다. 조건마다 새 세션을 열고, 세션 시작 전에 컨텍스트에 아무것도 남기지 않는다.
3. **캐시 토큰을 분리해서 본다.** `cache_read`는 단가가 낮고 `cache_write`는 높다. 단순 합계 외에 요금 환산 열을 따로 둔다.
4. **서브에이전트 사용량을 포함한다.** 하네스는 분석·리서치·빌드를 서브에이전트에 위임하므로 메인 세션만 세면 과소 집계된다. 서브에이전트 기록은 메인 세션 파일이 아니라 `~/.claude/projects/<프로젝트 폴더>/<세션ID>/subagents/agent-*.jsonl` 에 따로 저장되며, 스크립트가 이 폴더를 자동으로 찾아 `sub` 행에 합산한다. 이 프로젝트 세션으로 확인한 결과 메인 파일만 세면 서브에이전트 사용량이 0으로 나온다.
5. **N = 3 이상 반복**하고 중앙값을 보고한다. 리서치 결과와 인터뷰 질문 수가 실행마다 달라진다.
6. 성공 판정은 사람이 아니라 **`n8n_validate_workflow` 통과 + 사전에 적은 기대 노드·연결 체크리스트 일치** 로 한다.

## 비교 조건

| 조건 | 구성 | 세션 시작 방법 |
|------|------|----------------|
| A. 하네스 없음 | Claude Code + n8n-mcp 만 등록. `.claude/` 에 킷 없음, CLAUDE.md 에 트리거 포인터 없음 | 빈 프로젝트 폴더에서 `claude` |
| B. 하네스 있음 | A + 킷 설치 + CLAUDE.md 트리거 포인터 | 킷 설치된 폴더에서 `claude` |
| C. (선택) 범용 프레임워크 | A + oh-my-claudecode 같은 범용 오케스트레이터 활성 | 발표에서 타 도구 수치를 쓰지 않기로 했으므로 내부 참고용으로만 |

## 과제 세트 (S / M / L 각 1개 이상)

| ID | 등급 | 요청 문구 (두 조건에 동일하게 입력) | 성공 체크리스트 |
|----|------|-------------------------------------|-----------------|
| T1 | S | "결제 알림 워크플로우에 Slack 알림 노드 추가해줘" | Slack 노드 1개 추가, 기존 마지막 노드에서 연결, validate 통과 |
| T2 | M | "결제 알림 워크플로우에 금액 10만 원 이상만 Slack으로 보내고 나머지는 이메일로 보내게 분기해줘" | IF 노드 + 두 갈래, 조건식 정확, validate 통과 |
| T3 | L | "주문 webhook 받아서 필수 필드 검증하고, 실패하면 ops 채널 알림, 성공하면 DB 저장 후 고객 이메일 보내는 워크플로우 새로 만들어줘" | Webhook 트리거, 검증 분기, 실패 알림, DB 노드, 이메일 노드, validate 통과 |
| T4 | L (재사용) | 인스턴스에 `customer-notify` 가 있는 상태에서 "고객 알림 워크플로우 새로 만들어줘" | B 조건에서 재사용 선택지 제시 여부, 최종 워크플로우 validate 통과 |

## 실행 절차 (조건 × 과제 × 3회)

1. n8n 인스턴스를 기준 스냅샷으로 되돌린다 (기준 워크플로우 JSON 을 `n8n_update_full_workflow` 로 재적용하거나 인스턴스를 재생성).
2. 조건에 맞는 폴더에서 새 Claude Code 세션을 연다. 세션 ID 를 기록한다.
3. 과제 문구를 그대로 입력한다. 하네스가 질문하면 사전에 적어 둔 답변표대로 답한다 (두 조건에서 같은 정보를 준다).
4. 결과가 성공 체크리스트를 만족할 때까지 사용자 입장에서 수정 요청을 이어간다. 수정 요청 횟수를 기록한다.
5. 세션을 종료하고 아래로 집계한다.

```bash
# 세션 파일 위치: ~/.claude/projects/<프로젝트 폴더명>/<세션ID>.jsonl
python3 measure-tokens.py \
  ~/.claude/projects/-path-to-A/<세션A>.jsonl \
  ~/.claude/projects/-path-to-B/<세션B>.jsonl \
  --label 하네스없음 --label 하네스있음
```

한 세션에서 여러 과제를 이어서 돌렸다면 `--from "T2 문구" --to "T3 문구"` 로 구간을 잘라 집계한다.

## 결과 기록표 (실측 후 채움)

| 과제 | 조건 | 회차 | input | cache_write | cache_read | output | 서브에이전트 output | API 호출 | 수정 요청 수 | validate 첫 통과 | 소요 시간 |
|------|------|------|-------|-------------|------------|--------|---------------------|----------|--------------|------------------|-----------|
| T1 | A | 1 | | | | | | | | | |
| T1 | B | 1 | | | | | | | | | |
| … | | | | | | | | | | | |

요약표 (중앙값):

| 과제 | A 총 토큰 | B 총 토큰 | 차이 | A 요금 환산 | B 요금 환산 | 차이 | A 수정 요청 | B 수정 요청 |
|------|-----------|-----------|------|-------------|-------------|------|-------------|-------------|
| T1 | | | | | | | | |
| T2 | | | | | | | | |
| T3 | | | | | | | | |
| T4 | | | | | | | | |

요금 환산은 측정 시점의 모델 단가표를 붙여 두고 `input × 단가 + cache_write × 단가 + cache_read × 단가 + output × 단가` 로 계산한다.

## 예상되는 결과 형태와 발표에서의 사용

- S 과제(T1)는 하네스가 요건 확인 질문을 한 번 더 하므로 **B 가 A 보다 토큰을 더 쓸 가능성이 높다.** 이것은 정상이며, 발표에서는 "작은 작업에는 하네스 개입을 최소화한다"의 근거로 쓴다.
- M · L 과제에서 A 는 잘못된 파라미터·typeVersion 으로 인한 재작업이 늘어 총 토큰이 커질 가능성이 있다. B 의 이점은 **수정 요청 수와 validate 첫 통과율** 에서 먼저 나타나고, 총 토큰 차이는 그 다음이다.
- 발표 슬라이드에는 "토큰 n% 절감" 한 줄보다 **과제별 총 토큰 + 수정 요청 수** 두 열을 같이 보여준다. 실측이 끝나기 전에는 어떤 수치도 슬라이드에 넣지 않는다.

## 한계

- 이 프로토콜은 Claude Code 세션 기록에 의존한다. 기록 형식(`message.usage`, `requestId`, `isSidechain`)이 바뀌면 스크립트를 조정해야 한다.
- 사람이 답변표대로 응답하므로 완전한 자동화는 아니다. 답변표를 두 조건에 동일하게 유지하는 것이 재현성의 핵심이다.
