# 이미 느끼고 있던 그 답답함

Claude Code를 쓰다 보면 반드시 한계를 느끼는 순간이 오는거 같습니다..

처음에는 CLAUDE.md 하나로 다 해결했었지만 프로젝트 컨텍스트, 코딩 컨벤션, 금지 명령들. 그런데 작업이 복잡해질수록, 단일 Claude 인스턴스가 감당하기 어려운 상황이 생기기 시작합니담. 리서치를 하면서 동시에 코드를 짜야 하고, 짠 코드를 검증하면서 문서도 써야 하는 상황. 순차 처리로는 컨텍스트 창이 너무 빨리 차버리고, 앞에서 분석한 내용을 뒤에서 잊어버리더라구요...;;;;

그래서 에이전트 팀이라는 개념을 응용하기 시작했는데요.. 여러 Claude 인스턴스가 역할을 나눠 협업한다는 개념은 분명히 매력적인거 같습니다. 그리고 oh-my-opencode, oh-my-claudecode 같은 도구들이 정말 사용하기 좋더라구요. "멀티 에이전트 오케스트레이션!", "3.2배 빠름!", "40개 이상의 전문 에이전트!" 그럴싸한 숫자들이 가득해서, 필자도 spring camp 2026를 다녀오기 전까지 꾸준히 사용했었습니다;;

그런데 막상 써보면 뭔가 이상합니담. 단순한 작업인데 토큰이 왕창 나갑니다. 함수 이름 하나 바꾸는 작업 혹은 aws alb 보안 그룹 확인 정도인데 에이전트들이 회의를 열고 계획을 세우고 검증하죠. 그래서 작은 작업은 오히려 더 느리더라구요.

이 글은 그 답답함의 원인을 짚고, 더 나은 대안이 바로 **경량화된 도메인 특화 하네스**(GitHub: https://github.com/revfactory/harness)입니다. 무거운 범용 도구를 버리고, 내 도메인을 이해하는 작고 정밀한 에이전트 팀을 직접 설계하는 것. 하지만, claudecode가 제공하는 다양한 툴을 이해하고, 하네스를 설계한다는 것은 여간 쉬운일이 아니였습니담;;;

## Claude Code의 핵심 개념

본격적인 이야기 전에 모르시는 분들을 위해 용어를 정리해볼게요. Claude Code 에코시스템에서 자주 등장하는 4가지 개념입니다. 이것들을 제대로 이해해야 왜 경량화·도메인 특화가 중요한지 알 수 있습니다.

### Skills (스킬) — "어떻게 하는가"를 정의

스킬은 `.claude/skills/{name}/SKILL.md` 형태로 저장되는 절차적 지식 번들입니다.
특정 도메인의 작업 방법, 사용할 도구, 따라야 할 절차를 정의합니다. "어떻게 하는가"에 대한 답입니다.

중요한 것은 **트리거 메커니즘**입니다. 스킬의 `description` 필드에 특정 키워드를 포함시키면, Claude가 사용자 요청에서 해당 키워드를 감지했을 때 자동으로 스킬을 호출합니다. "PDF 파일 분석해줘"라는 요청이 오면 PDF 처리 스킬이 자동으로 활성화되는 식입니다. 이 트리거 메커니즘이 잘 설계된 스킬과 그렇지 않은 스킬의 차이를 만둡니다.

```markdown
# .claude/skills/pdf-handler/SKILL.md 예시
---
name: pdf-handler
description: "PDF 파일 읽기, 텍스트/테이블 추출, 병합, 분할, 변환. .pdf 파일을 언급하거나 PDF 산출물을 요청하면 반드시 이 스킬을 사용할 것."
---
```

### Hooks (훅) — 도구 실행 전후를 자동화

훅은 Claude가 도구를 실행하기 **직전(PreToolUse)** 또는 **직후(PostToolUse)**에 자동으로 실행되는 셸 명령입니다. `~/.claude/settings.json`에 설정하면 됩니당.

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "echo 'Bash 실행 전 검증'" }]
      }
    ]
  }
}
```

실용적인 예시들: 커밋 전 테스트 자동 실행, 파일 수정 전 백업 생성, 특정 위험 명령 차단, 도구 실행 후 로그 기록. 훅은 에이전트의 행동을 사람 개입 없이 제어하는 장치입니당. 특히 자동화된 에이전트 팀 환경에서는 훅이 안전장치 역할을 해줍니다.

### Agents (에이전트) — "누가 하는가"를 정의

에이전트는 `.claude/agents/{name}.md` 형태로 저장되는 전문가 페르소나입니다. 스킬이 "방법"을 정의한다면, 에이전트는 "담당자"를 정의한다. 동일한 코드 리뷰 작업이라도, 보안 전문가 에이전트와 성능 전문가 에이전트는 전혀 다른 관점으로 봅니다.

```markdown
---
name: code-reviewer
description: "코드 품질 검토 전문가. PR 리뷰, 보안 취약점 분석, 성능 병목 파악."
---

## 핵심 역할
당신은 시니어 보안 엔지니어로, 코드의 품질과 취약점을 검토합니다.

## 작업 원칙
- OWASP Top 10 기준으로 보안 취약점 우선 검토
- 성능 임팩트가 큰 순서로 피드백 정렬
- 수정 가능한 코드 예시 반드시 제시
```

Agent 도구로 명시적으로 호출(`@agent` 호출)하거나, 에이전트 팀을 구성할 때 자동으로 초대됩니다.

### Harness (하네스) — 이 모든 것을 묶는 팀 아키텍처

하네스는 Skills + Agents + Hooks + CLAUDE.md를 체계적으로 설계하고 연결하는 프레임워크입니다.
단순히 파일들을 모아두는 게 아니라, 에이전트들이 어떻게 협업할지, 어떤 데이터를 주고받을지, 오류 시 어떻게 복구할지를 설계합니다.

여기서 핵심 기능이 **에이전트 팀(Agent Teams)**입니다. `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 환경 변수를 설정하면, 여러 Claude Code 인스턴스가 공유 상태를 통해 실시간으로 협업할 수 있다. 팀원들이 `SendMessage`로 발견을 공유하고, `TaskCreate`로 공유 작업 목록을 관리합니다.

하네스는 이 팀을 설계하고 구성하는 PM 역할을 합니다.

## 기존 도구들의 문제: 과한 하네스의 역설

### Oh-My-OpenCode: 성능 역설

Oh-My-OpenCode는 OpenCode를 위한 멀티 에이전트 오케스트레이션 플러그인입니다. Sisyphus 오케스트레이터를 중심으로 Prometheus(계획), Oracle(디버깅), Librarian(문서 탐색), Explore(코드 분석) 에이전트를 운영하는데요. 들으면 그럴싸합니다.

| 항목 | Oh-My-OpenCode | 일반 OpenCode |
|------|----------------|---------------|
| 벤치마크 통과율 | 69.2% | 73.1% |
| 초기 토큰 오버헤드 | 15,000~25,000 | 거의 없음 |
| 작업 대비 비용 | 일반의 3배 | 기준 |

더 복잡하고, 더 비싸고, 오히려 더 느립니다. 이유가 뭘까요?

**모든 작업에 동일한 오케스트레이터를 적용하기 때문입니다.** "함수 이름 바꿔줘" 같은 단순 리팩토링에도 Sisyphus가 Prometheus에게 계획 수립을 요청하고, Oracle에게 컨텍스트를 확인하고, Librarian에게 관련 문서를 찾게 합니다. 오케스트레이션 오버헤드가 실제 작업보다 큽니다.

`ultrawork` 키워드를 명시적으로 사용하지 않으면 다중 에이전트의 이점조차 활성화되지 않고, 결국 무거운 컨텍스트의 일반 OpenCode처럼 작동한다. 최악의 조합인거 같습니다.

### Oh-My-ClaudeCode: 범용 스킬의 한계

Oh-My-ClaudeCode(OMC)는 Claude Code용 멀티 에이전트 프레임워크다. 40개 이상의 스킬과 32개 이상의 전문 에이전트를 제공합니다. 아키텍트, 실행자, QA 테스터, 보안 감시자, 디자이너까지. 패키지 하나에 모든 걸 넣으려고 합니다.

문제는 이 40개 스킬이 **매 세션마다 컨텍스트 창에 로드**된다는 점입니다. 저희(Claude code cli sonnet 만 사용 가능 유저 => opus보다 상대적으로 작은 컨텍스트 모델) 들은 가장 큰 문제입니다.

- 스킬 설명들이 컨텍스트를 오염시켜 본 작업 집중도 하락
- 오류 발생 시 응답 없이 토큰만 소비하는 경우

OMC가 내세우는 "3.2배 빠름", "30~50% 토큰 절감" 수치는 독립적인 제3자 벤치마크가 없다는 점입니다. OMC 자체 마케팅 자료에서 나온 수치입니다. 실제로 Oh-My-OpenCode의 독립 벤치마크에서는 성능이 오히려 하락했다는 점을 상기하면, 이런 마케팅 수치를 액면 그대로 믿기 어렵습니다.

더 근본적인 문제가 있습니다. **도메인 특화가 불가능하다는 것.**

차량, 금융 리스크 평가, 디지털키 플랫폼 API 테스트처럼 전문 도메인에서는 그 도메인을 깊이 이해하는 에이전트가 필요합니다. OMC의 "보안 감시자 에이전트"가 디지털키 인증서 체인 검증 흐름을 알 수 있을까? 범용 에이전트는 범용적인 답변만 합니다. "OWASP Top 10을 점검하세요"처럼.

### 공통 패턴: 오버엔지니어링의 함정

두 도구의 공통 문제를 정리하면 다음과 같다.

- **스킬이 많을수록 컨텍스트 오염 증가** — 관련 없는 스킬들이 Claude의 판단을 흐림
- **오버헤드가 에이전트 이점을 상쇄** — 단순 작업에서 오히려 손해
- **도메인 지식 없는 에이전트** — 전문 도메인에서 일반론만 반복
- **정책 리스크** — Anthropic의 서드파티 제한 정책에 언제든 취약

진짜 문제는 "에이전트 팀"이라는 개념 자체가 아닙니다. **도메인을 이해하지 않은 채 범용 도구를 쌓아 올리는 접근 방식**이 문제입니다.

## 경량화·도메인 특화 하네스가 필요한 이유

GitHub: https://github.com/revfactory/harness

모든 작업에 40개 스킬을 쏟아붓는 게 아니라, 지금 이 도메인에 필요한 것만 정밀하게 설계하는 것이라고 생각합니다.

### Progressive Disclosure: 필요할 때만 로드

revfactory/harness의 핵심 원칙 중 하나인 **Progressive Disclosure**는 이 문제를 우아하게 해결됩니다.

```
Metadata (~100단어)      → 항상 로드 (트리거 판단에만 사용)
     ↓ (트리거 감지 시)
SKILL.md 본문 (<500줄)   → 트리거 시 로드 (실제 작업 지침)
     ↓ (필요 시)
references/ (무제한)     → 조건부 로드 (심화 참조 자료)
```

기존 도구처럼 40개 스킬 전체를 항상 로드하지 않습니다. Claude가 사용자 요청을 보고 해당 스킬이 필요하다고 판단할 때만, 그 스킬의 본문을 로드합니다. 나머지는 Metadata만 메모리에 올라와 있습니다. 컨텍스트 창을 최소화하면서 필요한 전문 지식은 즉시 활성화할 수 있습니다.

### 도메인 특화의 힘

범용 에이전트 vs 도메인 특화 에이전트의 차이는 생각보다 훨씬 크다.

"코드 리뷰해줘"라는 요청에:

- **범용 에이전트:** SOLID 원칙, 가독성, 일반적인 보안 패턴을 확인한다
- **DKC 도메인 특화 에이전트:** LCBT BLE 통신의 타임아웃 처리, 인증서 체인의 유효성 검증 흐름, 언페어링 시 키 잔존 여부, TLV 파싱 오류 처리를 확인한다

같은 요청이지만 결과의 품질이 완전히 다르다. 도메인 지식이 내재된 에이전트는 "혹시 이 부분이 문제 아닐까요?" 수준이 아니라 "이 시나리오에서 이 코드는 반드시 이 방식으로 처리해야 합니다"를 말할 수 있다.

## revfactory/harness: 도메인 한 문장을 에이전트 팀으로

revfactory/harness는 Claude Code를 위한 **Team-Architecture Factory 메타 스킬**이다. "하네스 구성해줘"라는 요청 하나로, 도메인 설명을 분석하고 그에 맞는 에이전트 팀과 스킬 세트를 자동으로 생성한다. 빈 CLAUDE.md에서 시작해 완성된 에이전트 팀 구조까지, 한 번의 요청으로 도달한다.

### 4가지 핵심 설계 원칙

#### 1. 에이전트와 스킬은 반드시 파일로 생성한다

에이전트는 `.claude/agents/{name}.md`, 스킬은 `.claude/skills/{name}/SKILL.md`로 실제 파일을 생성한다. 대화 중에만 존재하는 설정이 아니라, Git으로 버전 관리가 가능한 영구적인 산출물이다. 팀원들과 공유하고, 변경 이력을 추적하고, 롤백도 가능

#### 2. 에이전트 팀을 기본 실행 모드로 사용한다

2개 이상의 에이전트가 협업이 필요할 때는 에이전트 팀(Agent Teams)을 기본으로 선택한다. 팀원들이 `SendMessage`로 실시간 발견을 공유하고, `TaskCreate`로 공유 작업 목록을 관리한다. 단순히 병렬 처리하는 게 아니라, 에이전트들이 서로의 발견에 반응하고 피드백을 주고받음.

#### 3. CLAUDE.md에는 하네스 포인터만 등록한다

CLAUDE.md는 무거운 도메인 지식을 직접 담는 게 아니라, 어떤 스킬을 언제 사용하라는 포인터(pointer)만 담음. 실제 도메인 지식은 스킬 파일에 있음. CLAUDE.md는 가볍게 유지하고, 변경 이력만 기록함.

#### 4. 하네스는 진화하는 시스템이다

처음부터 완벽한 하네스를 만들려고 하지 않음. 매 실행 후 어떤 에이전트가 잘 작동했는지, 어떤 스킬이 부족했는지 피드백을 수집하고 개선함. CLAUDE.md의 변경 이력이 이 진화 과정을 추적하며, 퇴행을 방지함.

### 6가지 팀 아키텍처 패턴

harness는 도메인 특성에 따라 6가지 팀 구성 패턴을 제공함니담. 단순히 에이전트를 만드는 게 아니라, 어떤 구조로 협업할 것인지를 설계하는거죠.

| 패턴 | 구조 | 설명 |
|------|------|------|
| 파이프라인 | A→B→C→D | 분석→설계→구현→검증의 순차 워크플로우 |
| 팬아웃/팬인 | A⊢{B,C,D}⊣E | 다각도 리서치 후 통합, 병렬 코드 리뷰 |
| 전문가 풀 | Router→{A\|B\|C} | 요청 유형에 따라 전문가 선택 호출 |
| 생성-검증 | 생성→검증→재시도 | 코드 생성 + QA 자동 반복 |
| 감독자 | 감독자⊢{워커들} | 중앙 관리자가 동적으로 작업 분배 |
| 계층적 위임 | 총괄→팀장→실무자 | 대규모 프로젝트의 계층별 위임 |

예를 들어 "다각도 리서치 팀"은 팬아웃/팬인 패턴이 가장 적합합니다. 웹 리서처, 기술 문서 리서처, 커뮤니티 동향 리서처가 병렬로 조사하고, 종합 에이전트가 발견을 통합하게됩니다.
에이전트들이 `SendMessage`로 실시간 발견을 공유하므로, 단순 병렬 처리보다 교차 검증이 일어나 품질이 높아집니다.

### 7단계 워크플로우

revfactory/harness가 하네스를 생성할 때 따르는 워크플로우인데요. 단순히 파일을 생성하는 게 아니라, 도메인 분석부터 검증까지 체계적인 단계를 거칩니다.

| 단계 | 이름 | 내용 |
|------|------|------|
| Phase 0 | 현황 감사 | 기존 에이전트/스킬 확인, 신규/확장/운영 분기 |
| Phase 1 | 도메인 분석 | 요청에서 핵심 작업 유형 파악, 기술 스택 탐색 |
| Phase 2 | 아키텍처 설계 | 실행 모드 선택(팀/서브/하이브리드) + 6패턴 중 선택 |
| Phase 3 | 에이전트 생성 | 중복 검토 후 `.claude/agents/{name}.md` 작성 |
| Phase 4 | 스킬 생성 | 중복 검토 후 `.claude/skills/{name}/SKILL.md` 작성 |
| Phase 5 | 오케스트레이션 | 오케스트레이터 스킬 + CLAUDE.md 포인터 등록 |
| Phase 6 | 검증 | 구조 검증 + 트리거 테스트 + 드라이런 |
| Phase 7 | 진화 | 피드백 수집 → 반영 → 변경 이력 기록 |

특히 **Phase 0의 현황 감사**와 **Phase 3-0, 4-0의 중복 검토**가 중요합니다. 처음 하네스를 만드는 건지, 기존 하네스를 확장하는 건지를 먼저 파악하고, 이미 있는 에이전트나 스킬을 중복 생성하지 않습니다.
이 단계가 있어야 하네스가 커질수록 관리하기 쉬워집니다.

## 실제 사용법

### 설치

```bash
# 1. 마켓플레이스에서 플러그인 추가
claude plugin marketplace add revfactory/harness

# 2. 설치
claude plugin install harness@harness-marketplace

# 3. 에이전트 팀 기능 활성화 (필수)
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1

# 쉘 설정 파일에 영구 추가
echo 'export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1' >> ~/.zshrc
```

### 첫 하네스 생성

Claude Code를 열고 한 문장으로 요청

```
/harness:harness DKC @{팀에서 정리한 문서: dkc_analyst_doc}폴더를 보고 사실기반으로 디지털키 플랫폼의 API 단위테스트 자동화 팀을 구성해줘. 페어링, 언페어링, 키공유 API를 검증하고 결과 보고서를 작성해야 해.
```

or

```
"@dkc_analyst_doc 폴더를 보고 사실기반으로 DKC 디지털키 플랫폼의 API 단위테스트 자동화 팀을 구성해줘. 페어링, 언페어링, 키공유 API를 검증하고 결과 보고서를 작성해야 해."
```

harness가 Phase 0부터 6까지 자동으로 실행하고, 다음과 같은 파일들을 생성합니다.

```
.claude/
├── agents/
│   ├── api-tester.md          # API 호출 및 응답 검증 전문가
│   ├── result-analyzer.md     # 테스트 결과 분석 및 패턴 파악
│   └── report-writer.md       # 테스트 보고서 작성 전문가
└── skills/
    └── dkc-api-test/
        ├── SKILL.md            # 테스트 절차, 환경 설정, 검증 기준
        └── references/
            ├── api-spec.md     # DKC API 명세 참조
            └── test-patterns.md # 도메인 특화 테스트 패턴
```

생성된 에이전트들은 사용자가 제공한 도메인 지식을 내재하고 있습니다. `api-tester.md`는 단순히 HTTP 요청을 보내는 게 아니라, DKC 페어링 플로우에서 TLV 파싱 오류, 세션 만료 처리, 인증서 체인 검증을 확인하는 방법을 이미 압니다. 범용 에이전트에서는 기대할 수 없는 수준입니다.

### 기존 하네스 확장

이미 구성된 하네스에 새 에이전트나 스킬을 추가할 때도 같은 방식으로 요청합니다. Phase 0에서 현황을 감사하고, 기존과 중복되지 않는 범위에서만 추가하게 됩니다.

```
"기존 DKC API 테스트 하네스에 성능 부하 테스트 에이전트를 추가해줘."
```

하네스 확장 시 매트릭스도 명확합니다. 에이전트 추가, 스킬 추가, 아키텍처 변경 각각에서 어떤 Phase를 실행해야 하는지 정의되어 있어, 불필요한 재작업을 최소화하죠

## 결론: 경량화된 도메인 특화가 답이다

oh-my-opencode와 oh-my-claudecode가 실패한 이유는 명확하다. 모든 도메인, 모든 작업에 동일한 무거운 구조를 적용하려 했기 때문이다. 40개 스킬이 있다고 해서 더 좋은 결과가 나오는 게 아니다. 지금 이 도메인에서, 지금 이 작업에 최적화된 스킬 5개가 훨씬 강하다.

revfactory/harness의 접근 방식은 근본적으로 다르다.

- 범용 스킬 묶음을 제공하는 게 아니라, **도메인 맞춤 에이전트 팀을 자동 설계**
- 처음부터 완성된 하네스를 만들려 하지 않고, **점진적으로 진화**하게 설계
- 40개 스킬을 항상 로드하는 게 아니라, **Progressive Disclosure**로 필요할 때만 로드
- 팀원들이 실시간으로 발견을 공유하는 **에이전트 팀을 기본**으로 사용

좋은 하네스는 처음부터 크게 태어나지 않는다. 도메인을 이해하는 작은 에이전트 몇 개로 시작해, 실제 작업을 통해 검증하고, 부족한 부분을 채워 나간다. 그 과정이 CLAUDE.md의 변경 이력에 남는다.
