# 스킬 명세 — n8n-workflow-reuse (기존 워크플로우 현황 감사 · 재사용 제안 · 조건부 딥 인터뷰)

n8n Orchestration Kit의 `n8n-workflow-manager` 절차에 끼워 넣는 확장 스킬 명세입니다.
킷 문서(readme / arch)에 없는 신규 기능이며, 이 문서는 구현 전 설계서입니다.

## 1. 목적

- 신규 워크플로우 생성 또는 큰 수정 요청이 들어오면, **인스턴스에 이미 있는 워크플로우를 먼저 훑어** 비슷한 것이 있는지 확인한다.
- 비슷한 것이 있으면 새로 만들지 않고 **Execute Sub-workflow 노드로 호출하는 재사용안**을 사용자에게 선택지로 제시한다.
- 작업 크기가 크거나 되돌리기 어려운 부수효과가 있으면 **기존 워크플로우 사실을 근거로 한 딥 인터뷰**로 요건을 좁힌 뒤 계획 단계로 넘긴다.
- 작은 요청에는 개입하지 않는다. reference.md가 지적한 "작은 작업에 큰 회의"를 만들지 않는 것이 첫 번째 제약이다.

## 2. 킷 절차에서의 위치

기존 7단계는 유지하고 1단계와 2단계를 확장한다.

```
1. 분석        n8n-workflow-analyst
   1-a 대상 워크플로우 JSON 읽기 (기존)
   1-b [신규] 인스턴스 현황 감사: 기존 워크플로우 목록 → 후보 추림 → 구조 비교
2. 요건 명확화  오케스트레이터
   2-a [신규] 작업 크기 분류 (S / M / L)
   2-b [신규] L 이면 딥 인터뷰, 재사용 후보가 있으면 재사용 여부 질문
   2-c 모호한 부분만 질문 (기존)
3. 요건 확인 (필수, 기존)
4. 리서치 (기존)
5. 계획 수립 (기존) — 재사용안이 채택되면 계획에 Execute Sub-workflow 연결이 포함됨
6. 실행 (기존)
7. 보고 (기존)
```

## 3. 파일 구성

```
.claude/
├── agents/
│   └── n8n-workflow-analyst.md        [수정] 1-b 현황 감사 절차 추가
└── skills/
    ├── n8n-workflow-manager/
    │   └── SKILL.md                   [수정] 2-a 분류, 2-b 분기 호출 추가
    └── n8n-workflow-reuse/            [신규]
        ├── SKILL.md                   후보 추림 기준, 유사도 판단 규칙, 질문 템플릿, 인터뷰 종료 조건
        ├── SIMILARITY_RULES.md        유사도 판정 규칙과 예시 (오탐/미탐 케이스 포함)
        ├── INTERVIEW_QUESTIONS.md     작업 유형별 인터뷰 질문 뱅크
        └── SUBWORKFLOW_GUIDE.md       Execute Sub-workflow / Trigger 노드 연결 방법 (get_node 로 최신 스펙 확인 절차 포함)
```

## 4. SKILL.md 초안

```markdown
---
name: n8n-workflow-reuse
description: n8n 워크플로우를 새로 만들거나 크게 수정하기 전에 인스턴스의 기존 워크플로우를 감사해 유사 워크플로우를 찾고, 있으면 Execute Sub-workflow 재사용안을 제시하며, 큰 작업이면 기존 워크플로우 사실을 근거로 딥 인터뷰를 진행한다. n8n-workflow-manager 의 1~2단계에서 호출된다. 사용자가 직접 부르지 않는다.
user-invocable: false
---

## 언제 실행하는가
n8n-workflow-manager 가 요청을 S / M / L 로 분류한 뒤:
- S: 이 스킬을 건너뛴다.
- M: 현황 감사(1-b)만 수행하고 재사용 후보가 있을 때만 질문한다.
- L: 현황 감사 + 딥 인터뷰를 모두 수행한다.

## 1-b 현황 감사 절차 (analyst 에게 위임)
1. n8n_list_workflows 로 목록을 받는다. 이름·태그·활성 여부만 본다.
2. 요청 문구의 키워드(도메인 명사, 외부 서비스명, 트리거 종류)와 이름·태그가 겹치는 후보를 최대 5개 추린다.
3. 후보만 n8n_get_workflow 를 structure 모드로 읽는다. full 모드는 쓰지 않는다.
4. SIMILARITY_RULES.md 기준으로 각 후보를 "재사용 가능 / 참고만 / 무관" 으로 판정하고 근거를 한 줄씩 남긴다.
5. 결과를 _workspace_n8n/<작업ID>/01b-audit.md 에 기록한다.

## 2-b 재사용 질문
"재사용 가능" 후보가 하나라도 있으면 AskUserQuestion 으로 묻는다. 자동으로 결정하지 않는다.
선택지:
  A. 기존 "<이름>" 을 Execute Sub-workflow 로 호출해 재사용 (권장)
  B. 기존 "<이름>" 을 복제해서 수정
  C. 새로 만든다
  각 선택지에 근거(공통 노드, 트리거, 차이점)를 한 줄로 붙인다.

## 2-b 딥 인터뷰 (L 만)
- INTERVIEW_QUESTIONS.md 에서 작업 유형에 맞는 질문을 고르되, 1-b 에서 확인한 사실을 선택지에 넣는다.
  예: "에러 처리는 기존 워크플로우 3개처럼 실패 시 ops 채널로 보낼까요?"
- 한 번에 최대 3개 질문. 답을 받으면 남은 모호 항목을 다시 센다.
- 종료 조건: 모호 항목 체크리스트(트리거, 입력 데이터, 성공 조건, 실패 처리, 부수효과 범위, 자격증명)가 모두 채워지거나, 사용자가 "그만"이라고 하거나, 질문 라운드가 3회에 도달하면 종료한다.
- 결과를 _workspace_n8n/<작업ID>/02b-interview.md 에 기록하고 3단계 요건 확인으로 넘긴다.

## 하지 않는 것
- 유사도 판단을 근거로 워크플로우를 자동 변경하지 않는다.
- 기존 워크플로우를 full 모드로 전부 읽지 않는다.
- S 등급 요청에 질문을 추가하지 않는다.
```

## 5. 작업 크기 분류 기준 (n8n-workflow-manager 2-a)

| 등급 | 조건 (하나라도 해당하면 상위 등급) | 이 스킬의 동작 |
|------|-----------------------------------|----------------|
| S | 기존 워크플로우에 노드 1~2개 추가·수정, 파라미터 변경, 표현식 수정 | 건너뜀 |
| M | 노드 3개 이상 추가, 분기(IF / Switch) 추가, 새 외부 서비스 연결 | 현황 감사만 |
| L | 신규 워크플로우 생성, 트리거 변경, 결제·삭제·대량 발송·외부 시스템 쓰기 같은 되돌리기 어려운 부수효과, 자격증명 신규 생성 | 현황 감사 + 딥 인터뷰 |

분류는 오케스트레이터가 요청 문구와 1-a 분석 결과로 판정하고, 판정 결과와 근거를 요건 확인(3단계) 요약에 한 줄로 포함해 사용자가 이의를 제기할 수 있게 한다.

## 6. 유사도 판정 규칙 (SIMILARITY_RULES.md 요지)

"재사용 가능" 판정은 아래를 모두 만족할 때만 내린다.

1. 트리거 종류가 같거나, 후보가 Execute Sub-workflow Trigger를 이미 갖고 있다.
2. 핵심 처리 노드(외부 서비스 노드, HTTP Request 대상 호스트, DB 테이블)가 요청과 절반 이상 겹친다.
3. 후보의 출력이 요청이 필요로 하는 데이터를 포함한다.

하나만 만족하면 "참고만"(인터뷰 선택지의 근거로만 사용), 아니면 "무관".
판정 근거는 반드시 노드 이름과 타입으로 적는다. "비슷해 보임" 같은 서술은 금지.

## 7. 사용하는 도구

| 단계 | 도구 | 비고 |
|------|------|------|
| 목록 | `n8n_list_workflows` | 필터·페이지네이션 사용, 이름·태그·활성 여부만 |
| 구조 읽기 | `n8n_get_workflow` (structure 모드) | 후보 최대 5개, full 모드 금지 |
| 노드 스펙 확인 | `get_node`, `validate_node` | Execute Sub-workflow 노드의 현재 typeVersion 과 파라미터를 실행 직전에 확인 |
| 질문 | `AskUserQuestion` | 재사용 여부, 딥 인터뷰 |
| 계획 반영 | 기존 `n8n_update_partial_workflow` / `n8n_create_workflow` | 재사용안 채택 시 Execute Sub-workflow 노드 추가, 후보에 Trigger 노드가 없으면 추가 작업을 계획에 포함 |

Execute Sub-workflow 노드와 Execute Sub-workflow Trigger 노드의 정확한 타입명·typeVersion 은 문서에 고정하지 않고 `get_node` 로 매번 확인한다. 킷의 "최신 문법 반영" 원칙과 같다.

## 8. 산출물

| 파일 | 내용 |
|------|------|
| `_workspace_n8n/<작업ID>/01b-audit.md` | 후보 목록, 판정, 근거 |
| `_workspace_n8n/<작업ID>/02b-interview.md` | 질문·답변, 남은 모호 항목, 종료 사유 |
| 3단계 요건 확인 요약 | 크기 등급, 재사용 선택 결과가 한 줄씩 포함 |

## 9. 트리거 테스트 케이스 (검증 단계용)

| 요청 문구 | 기대 등급 | 기대 동작 |
|-----------|-----------|-----------|
| "결제 알림 워크플로우에 Slack 노드 하나 추가해줘" | S | 스킬 건너뜀, 기존 게이트만 |
| "주문 데이터 받아서 검증하고 세 군데로 나눠 보내는 워크플로우 만들어줘" | L | 현황 감사 + 딥 인터뷰 |
| "고객 알림 워크플로우 새로 만들어줘" (인스턴스에 `customer-notify` 존재) | L | 재사용 후보 제시 (A/B/C 질문) |
| "webhook 트리거를 스케줄로 바꿔줘" | L | 딥 인터뷰 (트리거 변경) |
| "IF 노드 추가해서 금액 10만 원 이상만 통과시켜줘" | M | 현황 감사만, 후보 없으면 질문 없음 |
| "n8n 켜줘" | 해당 없음 | n8n-env-setup 으로 라우팅, 이 스킬 미호출 |

should-not: 위 S 케이스와 환경 요청에서 이 스킬이 호출되면 실패.

## 10. 위험과 완화

| 위험 | 완화 |
|------|------|
| 워크플로우가 많으면 목록·구조 읽기로 컨텍스트가 커짐 | 후보 5개 상한, structure 모드 고정, 이름·태그 1차 필터 |
| 유사도 오탐으로 잘못된 재사용 유도 | 자동 결정 금지, 근거를 노드 타입으로 명시, 사용자가 C(새로 만들기)를 항상 선택 가능 |
| 딥 인터뷰가 길어져 작은 요청까지 느려짐 | S 등급 제외, 라운드 3회·질문 3개 상한, 종료 조건 명시 |
| 재사용 후보에 Execute Sub-workflow Trigger 가 없어 호출 불가 | 판정 시 확인하고, 없으면 "Trigger 노드 추가"를 계획에 포함해 사용자에게 알림 |

## 11. 발표 자료와의 관계

presentation.md 섹션 8 "보완 중 추가한 기능" 슬라이드와 섹션 5 매핑 표의 1·2단계 확장 항목이 이 명세를 가리킨다. 이 명세는 설계 문서이며, 실제 킷에 구현·검증되었는지는 발표 전 체크리스트로 확인한다.
